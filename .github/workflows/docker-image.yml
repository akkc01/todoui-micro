name: Build and Push Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & push Image
        run: |
          docker build -t akkc01/todouimicro:${{ github.sha }} .
          docker push akkc01/todouimicro:${{ github.sha }}



      # # ye code Image ke tag ko change kar rha meri manifest repo me.
      # - name: Update Manifest Repo
      #   run: |
      #     git clone https://x-access-token:${{ secrets.MY_GITHUB_PAT }}@github.com/akkc01/GitOps-Argocd-manifest-portfolio.git
          
      #     cd GitOps-Argocd-manifest-portfolio/manifest-files
          
      #     sed -i "s|image: .*|image: akkc01/akkc:${{ github.sha }}|" deployment.yaml
          
      #     git config user.name "github-actions"
      #     git config user.email "actions@github.com"
          
      #     git add .
      #     git commit -m "Update image tag"
          
      #     git push
